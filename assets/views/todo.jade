ul.todos-list
  li.todo-item(ng-repeat='todo in todos | orderBy:"-created"')
    .todo-item-toggle
      input(type='checkbox', ng-model='todo.done', ng-change='todo.save()', id='todo-{{ todo._id }}')
      label(for='todo-{{ todo._id }}')

    .todo-item-content(ng-class='{ editing: todo == editedTodo }')
      .todo-item-edit.dropdown(ng-hide='todo == editedTodo', dropdown, dropdown-toggle-class='open')
        a.icon-cog(href='#', title='Edit')

        ul.dropdown-list
          li.dropdown-list-item
            a(href='#', ng-click='editTodo(todo)')
              i.icon-pencil
              | Edit
          li.dropdown-list-item
            a(href='#', ng-click='destroyTodo(todo)')
              i.icon-trash
              | Remove

      h4.todo-item-title {{ todo.title }}
      .todo-item-body {{ todo.body }}

      form.todo-editor-form(ng-submit='doneEditing(todo)')
        .form-line
          input.form-control(type='text', ng-model='todo.title', ng-trim='true', todo-focus="todo == editedTodo")
        .form-line
          textarea.form-control(rows='6', ng-model='todo.body', ng-trim='true')
        .form-line.form-buttons
          button.btn.btn-default(ng-submit='doneEditing(todo)') Discard
          button.btn.btn-success(ng-submit='doneEditing(todo)') Save

  li.todo-item.todo-blank-item
    .todo-blank-item-icon
      i.icon-cancel
    | Wohoo! You are done. Enjoy the rest of your day!
